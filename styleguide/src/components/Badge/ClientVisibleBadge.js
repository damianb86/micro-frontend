import React from 'react';
import Badge from './../../common/Badge';

const ClientVisibleBadge = () => <Badge colorClass="badge-client-visible" shapeClass="badge-round-on-top" />;

export default ClientVisibleBadge;
