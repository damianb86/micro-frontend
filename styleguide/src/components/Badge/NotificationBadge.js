import React from 'react';
import Badge from '../Badge';

const NotificationBadge = () => <Badge colorClass="badge-unread_notifications" shapeClass="badge-round-on-top" />;

export default NotificationBadge;
